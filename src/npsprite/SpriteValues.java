package npsprite;

/**
 * Thought we wouldn't need this with animations, but apparently we do.
 * @author Wendy
 *
 */
public class SpriteValues
{
    public enum DIR{
        LEFT, RIGHT
    }
    public enum STATUS{
        PUNCH, BLOCK, NORM
    }
}
